@model IEnumerable<ClinicForAnimal1._2.Models.PatientCart.PatientViewModel>

<div id="searchresults">
   
   @if (Model.Count() > 0)
   {
    <ul>
        @foreach (var item in Model)
        {
            <li>@item.Diagnosis</li>
            <li>@item.Treatment</li>
        }
    </ul>
   }
   
</div>